import React from 'react';
//side bar has two views: it has the filecabinet view 
//the file cabinet view can look different if a folder is selected
// The file cabinet view has an add folder button
//the last view is for selected notes: it has a "Go Back" button
//selected-notes view also displays which folder the user is in curently

export default function Sidebar(props) {
  
}